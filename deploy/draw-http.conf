# Bootstrap HTTP-01 validation only; replaced with draw.conf after issuance.
server {
    listen 80;
    server_name draw.wedding-en.ru;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    location / { return 404; }
}
